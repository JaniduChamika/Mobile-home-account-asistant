<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Shadow effect using Gradient -->
    <solid android:color="@color/light_blue_theam" /> <!-- Background color -->
    <corners android:radius="30dp" />  <!-- Rounded corners -->
    <padding
        android:bottom="8dp"
        android:left="5dp"
        android:right="5dp"
        android:top="8dp" />
    <stroke
        android:width="0dp"
        android:color="#ededed" />

</shape>
